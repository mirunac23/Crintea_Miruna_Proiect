@page
@model Crintea_Miruna_Proiect.Pages.Skaters.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Skater[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Skater[0].Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Skater[0].Height)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Skater[0].EmailAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Skater[0].SkatingClub.Name)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Skater) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Height)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SkatingClub.Name)
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
